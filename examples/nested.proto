syntax = "proto3";
package validator.examples;
import "bitbucket.org/holdex/go-proto-validators/validator.proto";

message InnerMessage {
  // some_integer can only be in range (1, 100).
  InType test = 1 [(validator.field) = {int_gt:1 is_in_enum:true}];
  repeated InType test_array = 2 [(validator.field) = {is_in_enum:true}];
}

enum InType {
  ANY = 0;
  Test = 1;
}